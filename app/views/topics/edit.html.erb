<h1> Edit The Topic </h1>

<div class="row>
  <div class="col-md-4">
    <p>Guidelines for topics:</p>
    <ul>
      <li>Make sure the topic is appropiate for dogs, cats and mice.</li>
      <li>Never insult dogs, dogs are nice, cats are not, mice are bleh.</li>
      <li>Smile when you type, like a happy dog, chasing a cat, chasing a mouse.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @topic do |f| %>
    <div clas="form-group">
      <%= f.label :name %>
      <%= f.text_field :name, class: 'form_control', placeholder: "Enter Your Topic Name Here..." %>
    </div>
    <div clas="form-group">
      <%= f.label :description %>
      <%= f.text_area :description, rows: 8, class: 'form_control', placeholder: "Enter Your Topic Description Here..." %>
    </div>
    <div clas="form-group">
        <%= f.label :public, class: 'checkbox' do %>
          <%=f.check_box :public %> Public Topic
        <% end %>
      </div>
      <%= f.submit "Save", class: 'btn btn-success' %>
    <% end %>
  </div>
</div>